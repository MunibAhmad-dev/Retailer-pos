# Leave blank for local development (Vite proxy handles /api → localhost:4000)
# Set this for production deployment (Vercel / Netlify / Railway static site)
VITE_API_URL=https://pos-backend-cloud-production.up.railway.app/api
