# Prevent npm from hoisting React Native packages to the parent Electron project.
# The reactnative-pos project MUST have its own isolated node_modules.
legacy-peer-deps=true
install-strategy=nested
workspaces=false
